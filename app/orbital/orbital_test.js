'use strict';

describe('myApp.orbital module', function() {

  beforeEach(module('myApp.orbital'));

  describe('office controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var orbitalCtrl = $controller('OrbitalCtrl');
      expect(orbitalCtrl).toBeDefined();
    }));

  });
});