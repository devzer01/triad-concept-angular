'use strict';

describe('myApp.simulation module', function() {

  beforeEach(module('myApp.simulation'));

  describe('office controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var orbitalCtrl = $controller('SimulationCtrl');
      expect(orbitalCtrl).toBeDefined();
    }));

  });
});